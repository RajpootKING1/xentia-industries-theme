<section class="error-404-section text-center" style="padding: var(--space-20) var(--space-4);">
  <div class="container" style="max-width: 600px;">
    <span class="badge-pill">ERROR 404</span>
    <h1 style="font-family: var(--font-heading); font-size: var(--text-5xl); color: var(--color-steel-white); margin: var(--space-4) 0;">INSTRUMENT NOT FOUND</h1>
    <p style="color: var(--color-steel-silver); font-size: var(--text-base); margin-bottom: var(--space-8);">
      The surgical instrument pattern, tray configuration, or page you requested could not be located in our active factory catalog.
    </p>
    <div style="display: flex; gap: var(--space-4); justify-content: center; flex-wrap: wrap;">
      <a href="/" class="btn btn-primary btn-sm">RETURN TO STOREFRONT</a>
      <a href="/collections/all" class="btn btn-secondary btn-sm">BROWSE FULL CATALOG</a>
    </div>
  </div>
</section>
